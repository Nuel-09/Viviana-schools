# School logo

Official logo file: **`logo.jpg`** (from Viviana Private Schools crest).

This file is served at `/images/logo.jpg` and is used in the header, footer, favicon, and SEO metadata.

To update the logo, replace `logo.jpg` in this folder (keep the same filename) or change `logo.src` in `src/lib/site.ts`.
